import Ember from 'ember';
import layout from '../../templates/components/stream-carousel/header';

export default Ember.Component.extend({
  layout,
  tagName: 'h2',
  classNames: ['slide-header']
});
